/* ==========================================================================
   Outsight Tokyo — 설정 파일
   이 파일의 값 4개만 채우면 됩니다. 나머지 파일은 손대지 마십시오.
   ========================================================================== */

window.OUTSIGHT_CONFIG = {

  /* ---- 1. 구글 지도 ------------------------------------------------------
     Google Cloud Console에서 만든 API 키를 붙여넣으십시오.
     설정 방법은 SETUP.md 1단계 참고.                                        */
  GOOGLE_MAPS_KEY: "",

  /* ---- 2. Supabase (답사 기록 동기화) ------------------------------------
     비워 두면 기록이 이 기기(브라우저)에만 저장됩니다.
     값을 채우면 휴대폰·노트북 사이에서 기록이 공유됩니다.
     설정 방법은 SETUP.md 2단계 참고.                                        */
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",

  /* ---- 3. 내 기록의 이름표 (열쇠) -------------------------------------
     추측하기 어려운 아무 문자열. 이게 곧 기록의 열쇠입니다.
     예: "jieun-tokyo-2026-x7q2m9"

     ★ GitHub Pages처럼 소스가 공개되는 곳에 올린다면 여기는 비워 두십시오.
       대신 주소 뒤에 #id=내열쇠 를 붙여 한 번만 열면 기기에 저장되고
       주소창에서는 즉시 지워집니다. 예:
         https://아이디.github.io/저장소/#id=jieun-tokyo-2026-x7q2m9
       휴대폰과 노트북에서 각각 한 번씩만 이렇게 열면 됩니다.

     Netlify·Cloudflare처럼 소스가 공개되지 않는 곳이면 여기에 적어도 됩니다.
     어느 쪽이든 SETUP.md 2단계 SQL의 값과 반드시 같아야 합니다.            */
  NOTEBOOK_ID: "",

  /* ---- 4. 부가 기능 (선택) -----------------------------------------------
     STREET_VIEW  : 장소를 열면 스트리트뷰로 건물 외관을 보여줍니다.
     PLACE_PHOTOS : 구글이 가진 장소 사진을 함께 보여줍니다.
                    Places API를 따로 켜야 하고, 월 무료 한도가 지도보다
                    적습니다. 부담되면 false로 두십시오.                     */
  STREET_VIEW: true,
  PLACE_PHOTOS: true
};
